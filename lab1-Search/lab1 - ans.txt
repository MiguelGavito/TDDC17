1. there is no so much difference between the 2 in a domain where the cost of each action is 1. On this domain the acumulated cost and sum of jumps work the same for the election.

2.  A (h1 + h2)/2, and C max(h1;h2), the max of h1 or h2 is always admisible no matter which one and the half of h1 and h2 is always going to be the half or minor than the adminisible. 

3.  


cambios al codigo de bfs y astar

el bfs se metio una fila queue para ir explorando las rutas,
guarda su estado inicial y el nodo actual para luego a;adir al queue frontier ese nodo
con llo luego se gaurda a visited states el estado inicial

un while loop mientras frontier no este vacio
definimos el nodo actual sacando el primero de frontier
sacamos el estado inicial 

checamos si estamos en la meta 
en caso de serlo terminamos y armamos la ruta, eso lo hacemos aramando una lsita con los nodos y avanznado a sus padres hasta llegar al inicio
luego lo invertimos

si no es
a;adirmos a explored el nodo y marcamos al sucesor
y a;adimos el sucesor a el frontier.
tambien hacemos la escepcion para revisar que el sucesor no fue visitado previamente
y a;adimos a foriner el siguiente y a;adimos a los visitados


el A* es similar pero con peque;os cambios en forma, este toma camino por menor costo de ruta con su heuristica
para la heuristica uso la distnacia de manhatan hacia la meta y la clase nueva hereda de searchnode pero agregnaod su heuristica

el contador esta para los casos donde 2 nodos tienen la misma heuristica, elige al de menor contador para tener un orden en ese caso,
la prioridad siendo la heuristica es el costo sumandole la heuristica
despues de eso metemos al heapq frontier el nodo, esta estructura saca al mas barato primero o menor por eso lo usamos

el best g lo usamos para guardar el mejor costo conocido

en el while loop igual de frontier mientras no este vacio
se saca del frontier, definimos el nodo y sacamos el estado actual

revisamos si estamos en la meta  para realizar el camino de regreso

si no a;adimos a explored, obtenemos los succesores y a;adimos los sucesores a frontier
igualcon la condicion de que el sucesor no se encuentre en los guardados de best g y no sea mayor o igual a best g succesor, osea no este ni haya estado

aqui calculamos la heuristica  y sacamos la euristica
sacamos la prioridad para el frontier y a;adirmos al contador para evitar duda de cual elegir